# Web (uvicorn without [standard] to avoid heavy deps that build from source)
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.9

# Data
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.0

# AI / Agent (use OpenAI or set OPENAI_API_KEY)
openai>=1.12.0

# Env
python-dotenv>=1.0.0
